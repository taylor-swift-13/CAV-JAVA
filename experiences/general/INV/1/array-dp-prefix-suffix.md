# 数组 / DP 题：invariant 先分「已定义前缀」和「未定义后缀」

如果循环逐步写数组，优先把内存分成两段：

- 已经写好的前缀：`*_seg` 或对应完整语义
- 尚未写入的后缀：`undef_seg`

处理方法：

- 先让 shape 上闭合
- 再在已写前缀上叠加值语义

不要一开始就试图把整段数组都写成完整值语义；这很容易在初始化早期就卡住 invariant 校验。
