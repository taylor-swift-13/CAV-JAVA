# Proof Reasoning

## Initial manual obligations after fresh symexec

Fresh `symexec` on `annotated/verify_20260422_214114_reverse_digits.c` succeeded and generated five manual obligations in `coq/generated/reverse_digits_proof_manual.v`:

```coq
Lemma proof_of_reverse_digits_safety_wit_3 : reverse_digits_safety_wit_3.
Lemma proof_of_reverse_digits_safety_wit_5 : reverse_digits_safety_wit_5.
Lemma proof_of_reverse_digits_entail_wit_1 : reverse_digits_entail_wit_1.
Lemma proof_of_reverse_digits_entail_wit_2 : reverse_digits_entail_wit_2.
Lemma proof_of_reverse_digits_entail_wit_3 : reverse_digits_entail_wit_3.
```

The current generated stubs are all `Proof. Admitted.`, so `proof_manual.v` is not acceptable. The generated goals are pure arithmetic and Coq-helper goals rather than missing heap shape:

```coq
reverse_digits_acc_z n ans = reverse_digits_z n_pre
```

is available at the loop head. For a positive loop iteration, the proof needs to rewrite that relation to the post-body state:

```coq
reverse_digits_acc_z n ans =
reverse_digits_acc_z (n ÷ 10) (ans * 10 + n % 10)
```

Then the generated overflow witnesses can be proved by showing the new accumulator is nonnegative and no larger than the final value `reverse_digits_z n_pre`, which the contract bounds by `INT_MAX`.

Planned proof edit:

```coq
Lemma reverse_digits_acc_z_step :
  forall n acc,
    0 < n ->
    reverse_digits_acc_z n acc =
    reverse_digits_acc_z (n ÷ 10) (acc * 10 + n % 10).
```

This helper unfolds `reverse_digits_acc_z` to `reverse_digits_acc_fuel`, consumes one fuel step when `n > 0`, and then uses a stability lemma to replace the remaining fuel by `Z.to_nat (n ÷ 10)`. Additional helper lemmas record `0 <= n ÷ 10 < n`, nonnegative remainder, initialization, zero-exit, and accumulator monotonicity. The witness proofs then use `pre_process`, `entailer!`, `rewrite`, and `lia`/`nia`.

## Compile iteration 1: fuel-stability side-condition order

First compile attempt stopped in `reverse_digits_acc_fuel_stable`:

```text
File ".../reverse_digits_proof_manual.v", line 67, characters 10-22:
Error:
...
Unable to unify
 "((?M3088 <= ?M3089)%nat -> Z.of_nat ?M3088 <= Z.of_nat ?M3089) /\
  (Z.of_nat ?M3088 <= Z.of_nat ?M3089 -> (?M3088 <= ?M3089)%nat)" with
 "(0 ?= n / 10) = Gt -> False".
```

The failing script was:

```coq
rewrite IHfuel; auto.
apply Nat2Z.inj_le.
```

The `auto` did not discharge the same side conditions as in the archived proof; after the induction rewrite, the focused goal was the quotient nonnegativity premise, not the natural fuel bound. The repair is to avoid relying on generated subgoal order: explicitly assert the quotient fuel bound

```coq
Hq_fuel : (Z.to_nat (n ÷ 10) <= fuel)%nat
```

from `0 <= n ÷ 10 < n` and `Z.to_nat n <= S fuel`, then call the induction hypothesis with both side conditions named.

## Compile iteration 2: Coq `Z.div` in spec versus C quotient in VCs

The next compile failed at the explicit induction rewrite:

```text
Found no subterm matching
"reverse_digits_acc_fuel (n ÷ 10) (acc * 10 + n % 10) (fuel + extra)"
in the current goal.
```

`coqtop` showed that after unfolding `reverse_digits_acc_fuel`, the recursive call uses `n / 10`, because the input Coq file defines the recursive step with `Z.div`. The generated C VCs use `n ÷ 10` for C integer division. For nonnegative `n` and positive divisor `10`, `Z.quot_div_nonneg` gives `n ÷ 10 = n / 10`, so the proof must use `/` in the fuel-stability and accumulator-growth lemmas, then explicitly replace `n / 10` with `n ÷ 10` in `reverse_digits_acc_z_step`.

## Compile iteration 3: quotient/division replacement direction

The next compile stopped at the replacement side condition:

```text
Unable to unify "?M3060 ÷ ?M3061 = ?M3060 / ?M3061" with
"n / 10 = n ÷ 10".
```

The side condition generated by `replace (n / 10) with (n ÷ 10)` is `n / 10 = n ÷ 10`, while `Z.quot_div_nonneg` states the reverse direction. I will rewrite the quotient occurrence directly:

```coq
rewrite Z.quot_div_nonneg by lia. reflexivity.
```

## Fresh witnesses after annotation bridge

After adding `/*@ n <= INT_MAX by local */`, `symexec` was rerun and regenerated the proof files. The manual witness list changed:

```coq
proof_of_reverse_digits_safety_wit_3
proof_of_reverse_digits_safety_wit_5
proof_of_reverse_digits_entail_wit_2
proof_of_reverse_digits_entail_wit_3
proof_of_reverse_digits_entail_wit_4
```

The new `entail_wit_1` is the local-store bridge and is generated in `proof_auto.v`; manual proof must not duplicate it. The old initialization proof now applies to `entail_wit_2`, the loop-preservation proof to `entail_wit_3`, and the loop-exit proof to `entail_wit_4`.

## Compile iteration 4: initialization witness auto-closes after bridge

After applying the proof to the regenerated file, compile stopped at `proof_of_reverse_digits_entail_wit_2` with:

```text
Error: No such goal.
```

The failing point was `entailer!` immediately after `pre_process`. The new local bridge gives the initialization VC enough pure facts that `pre_process` closes it completely. I will reduce this witness to:

```coq
Proof.
  unfold reverse_digits_entail_wit_2.
  pre_process.
Qed.
```
