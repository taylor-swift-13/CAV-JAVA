# Add Two Numbers

## 问题描述

实现一个函数，输入两个整数 `a` 和 `b`，返回它们的和。

约定：

- 输入都在 32 位有符号整数范围内。
- 结果也保证在 32 位有符号整数范围内。
- 函数不读写任何额外内存。

## 正确代码

```c
int add_two(int a, int b) {
    return a + b;
}
```

## 说明

这是一道最简单的纯标量函数：

- 没有循环
- 没有数组
- 没有额外内存资源
- 后置条件只需要表达返回值等于 `a + b`
